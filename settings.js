export const Settings = {

    cylinderWidth: 30,
    cylinderHeight: 50,
    cylinderSegment: 32,

    color: 0x6666ff,
    lightIntensity: 3,

    sphereRadius: 34,
    sphereCount: 9,
    sphereSegment: 64,
    sphereOffset : 2, // multiply, make the sphere further from the cylender center

    bottomOffset : 5,
    layerCount: 4, // 4
    verticalLayerOffset: 0.8, // offset between each layer
    verticalRadiusOffset : 1.01, // rotate each sphere layer

}
